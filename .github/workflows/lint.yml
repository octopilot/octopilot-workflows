name: "Lint (Octopilot)"

on:
    workflow_call:
        inputs:
            python-version:
                description: "Python version to use for pre-commit."
                required: false
                type: string
                default: "3.12"
            extra-args:
                description: "Additional arguments for pre-commit run."
                required: false
                type: string
                default: "--all-files"
            skip-checks:
                description: "Comma-separated list of pre-commit hooks to skip (SKIP env var)."
                required: false
                type: string
                default: ""
            languages:
                description: "Comma-separated list of languages to setup (go, rust, node, python)."
                required: false
                type: string
                default: ""
            go-version:
                description: "Go version to use"
                required: false
                type: string
                default: "stable"
            rust-version:
                description: "Rust version/toolchain to use"
                required: false
                type: string
                default: "stable"
            node-version:
                description: "Node version to use"
                required: false
                type: string
                default: "lts/*"
            java-version:
                description: "Java version to use"
                required: false
                type: string
                default: "17"

jobs:
    lint:
        name: Lint
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-python@v5
              with:
                  python-version: ${{ inputs.python-version }}

            # --- Multi-language Setup ---

            - name: Setup Go
              if: contains(inputs.languages, 'go')
              uses: actions/setup-go@v5
              with:
                  go-version: ${{ inputs.go-version }}

            - name: Setup Rust
              if: contains(inputs.languages, 'rust')
              uses: dtolnay/rust-toolchain@master
              with:
                  toolchain: ${{ inputs.rust-version }}
                  components: rustfmt, clippy

            - name: Setup Node
              if: contains(inputs.languages, 'node')
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ inputs.node-version }}

            - uses: actions/setup-python@v5
              if: contains(inputs.languages, 'python')
              with:
                  python-version: ${{ inputs.python-version }}

            - uses: actions/setup-java@v4
              if: contains(inputs.languages, 'java')
              with:
                  distribution: "temurin"
                  java-version: ${{ inputs.java-version }}

            - name: Install pre-commit
              run: pip install pre-commit

            - name: Cache pre-commit
              uses: actions/cache@v4
              with:
                  path: ~/.cache/pre-commit
                  key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml') }}
                  restore-keys: |
                      pre-commit-${{ runner.os }}-

            - name: Run pre-commit
              env:
                  SKIP: ${{ inputs.skip-checks }}
              run: pre-commit run ${{ inputs.extra-args }}
